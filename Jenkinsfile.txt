pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'dotnet build SmartRefridgerator.sln -p:configuration=release -v:n'   
            }
        }

    }
}
